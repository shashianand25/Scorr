import React from "react";
import type { HomeScreenProps } from "../../types/HomeScreenProps";

/** Renders all library-related modals if any. */
export function LibraryAddModals({ p }: { p: HomeScreenProps }) {
  return null;
}
